<div class="header-container index">
  <h1>My Lists<h1>
</div>

<div class="main-container">
  <%= link_to "Create a new list", new_list_path, class: "button primary"%>

  <div class="card-container">
  <% @lists.each do |list| %>
    <%= link_to list.name, list, class: "list-card" %>
  <% end %>
  </div>

</div>
