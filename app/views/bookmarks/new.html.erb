<h1>New Bookmark</h1>

<%= simple_form_for([@list, @bookmark]) do |f| %>
  <%= f.association :movie %>
  <%= f.input :comment %>
  <%= f.submit "Create bookmark", class: "btn btn-primary" %>
<% end %>

<%= link_to "Go back", lists_path, class: "btn btn-outline-primary" %>
